import React from "react";

function filator() {
  return alert("bravo !");
}

function Button() {
  return (
    <div>
      <button onClick={filator}>clique sur moi!</button>
    </div>
  );
}

export default Button;
